"# a4.office.products"

## Agent Pack execution

The authoritative pack is `agent_pack/`. Use `agent_pack/prompts/COPY_THIS_PROMPT.md` and set `RUN_STEP_COUNT` to `1` or `2`. The active frontend modernization phase begins at Step 061 and is locked to the existing template style with A4 blue/navy branding, responsive behavior, light/dark modes, and Arabic-first localization.
