# Deployment Notes

- Frontend: React + Vite build.
- Backend: Node.js + Express.
- Database: SQLite file with migration process.
- Configure database path via environment variable, for example `SQLITE_DB_PATH`.
- Keep backups for the SQLite database file before deployments.
- Currency: EGP.
- Timezone: Africa/Cairo.
- App UI: Arabic RTL.
