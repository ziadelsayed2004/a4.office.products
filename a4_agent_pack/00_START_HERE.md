# Start Here — A4 Office Products

1. Open `agent_pack/prompts/COPY_THIS_PROMPT_AR.md`.
2. Copy the full prompt into the coding agent.
3. The agent must read `ONE_PROMPT_RUNNER.md`.
4. The agent must execute exactly one open/pending step from `status.json`.
5. The app must remain Arabic RTL.
6. The database target is SQLite only.
