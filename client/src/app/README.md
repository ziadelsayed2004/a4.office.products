# Application Bootstrap Core

This folder configures the root application setup:
- `providers/` — MUI Theme, RTL Cache, Auth contexts, localization wrapper.
- `router/` — Routing tables.
- `guards/` — ProtectedRoute checks, AdminRoute role checks.
